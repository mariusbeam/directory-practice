import { Plugin } from 'rollup';

interface ViteTomlOptions {
    namedExports?: boolean;
}
declare const ViteToml: ({ namedExports }?: ViteTomlOptions) => Plugin;

export { ViteToml, type ViteTomlOptions };
